type User {
    id: ID!
    name: String!
    email: String!
    friends: [User]!
}

type Query {
    users(err: Boolean): [User]
    user(id: ID!, err: Boolean): User!
}

type Mutation {
    changeName(newName: String!): User!
}
