type User {
    id: ID!
    name: String!
    email: String!
    friends: [User]!
}

type Query {
    users(err: Boolean): [User]
    authUser(id: ID!, err: Boolean): User!
}

type Mutation {
    changeName(newName: String!): User!
}
